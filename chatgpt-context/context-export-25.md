# CHATGPT CONTEXT EXPORT — DEEP SWEEP INDEX / ADDENDUM

Дата: 2026-09-04.

Этот файл дополняет `context-export-20.md`, который был создан до дополнительного прохода по долговременной памяти.

## Новые части после deep-memory sweep

### `context-export-21.md`
PromptDeck / ChatGPT Control:
- 120-командная спецификация;
- `chatgpt_image_control_panel.html`;
- `GPT_Pult_mobile.html`;
- PWA и GitHub repo `gffvj5898h-design/promptdeck`;
- GitHub Pages;
- self-contained prompt architecture v0.2;
- незавершённые photo/share/settings задачи;
- решение не опираться на отдельный OpenAI API в этой схеме.

### `context-export-22.md`
Сельмаш — дополнительный operational memory:
- финальная стадия УПП → ERP;
- производственное планирование/себестоимость/НЗП/сменные отчёты;
- кадровый кризис: 18 из 25 грузчиков подали заявления;
- 3-месячный суммированный учёт;
- correction: ООО «Сельмаш биотехнологии» — деревообработка;
- понедельничная management orientation;
- отдельный payroll-case, который нельзя путать с личным доходом Романа.

### `context-export-23.md`
Профстрой — точный GitHub-срез:
- PR #9 REG-BASE;
- PR #12 Codex/site/integration foundation;
- PR #18 clean procurement portal MVP;
- структура и status PR;
- подтверждённые метрики набора данных;
- конфликт 5 977 vs 7 328 unique nomenclature items;
- read-only/private portal architecture.

### `context-export-24.md`
Multi-agent / GitHub coordination:
- immutable message files;
- `coordination/state.json`;
- `next_actor` authoritative;
- GitHub Actions handoff validation;
- правила ветки `grok-training-context-2026-08-22`;
- `memory_unverified` для memory-only facts;
- one fact / one source;
- broker как sole writer canonical state;
- единственный canonical face Алисы — `character/references/alice-master-face.jpg`.

## Дополнительные открытые ветки после deep sweep

### PromptDeck
Открыты:
- photo upload/preview;
- format/style/variant settings;
- full self-contained prompt generation;
- share photo + prompt;
- presets;
- scenario-first home;
- service-worker cache versioning;
- отдельный backend-контур генерации изображения, если проект продолжится.

### Profstroy
Состояние важно читать из GitHub, потому что PR #9/#12/#18 на момент проверки остаются open/not merged. Нельзя считать их содержимое автоматически частью `main`.

### Алиса / multi-agent
Перед любым продолжением читать текущие repository instructions/protocol/state. Исторический экспорт — контекст, но не замена current GitHub state.

### Claude migration
`roman-claude-context` остаётся private. Доступ ChatGPT и доступ Claude — отдельные GitHub authorization/installations; права одного клиента не переносятся на другой.

## Обновлённый порядок чтения для другой LLM
1. `context-export-25.md` — этот addendum.
2. `context-export-20.md` — основной старый index.
3. `context-export-1.md` — master context.
4. `context-export-2.md`–`context-export-19.md` — история и артефакты.
5. `context-export-21.md`–`context-export-24.md` — deep-memory supplement.

## Принцип доверия
При конфликте:
1. последнее прямое сообщение Романа;
2. текущий primary file / GitHub source of truth;
3. более ранний primary document;
4. сохранённая memory reconstruction;
5. assistant-derived recommendation.

Deep-memory sweep не должен превращать старую память в актуальный факт без проверки.
